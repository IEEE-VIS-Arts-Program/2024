<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{title}}</title>
<link rel="stylesheet" href="{{ '/styles/bundle.css' | url}}">
</head>
<body class="has-navbar-fixed-top">
{% include "navbar.njk" %}
<section class="section">
  <div class="columns">
    <div class="column is-8 is-offset-2">
      <div class="content">
        <h1>{{ title}}</h1>
        {{ content | safe }}
        <h2>Artwork</h2>
        {%  set sortedContributors = collections.contribution | sortContributions %}
        {% for post in sortedContributors %}
          {% if post.data.type == "artwork" %}
            <p><a href="{{ post.url }}">{{ post.data.title }}</a>{{ post.data.type }}</p>
          {% endif %}
        {% endfor %}
        <h2>Paper</h2>
        {% for post in sortedContributors %}
          {% if post.data.type == "paper" %}
            <p><a href="{{ post.url }}">{{ post.data.title }}</a>{{ post.data.type }}</p>
          {% endif %}
        {% endfor %}
        <h2>Pictorial</h2>
        {% for post in sortedContributors %}
          {% if post.data.type == "pictorial" %}
            <p><a href="{{ post.url }}">{{ post.data.title }}</a>{{ post.data.type }}</p>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>
{% include "footer.njk" %}
<script src="{{ '/scripts/scripts.js' | url }}"></script>
</body>
</html>